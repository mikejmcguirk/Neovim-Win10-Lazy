I am a very basic md file.

Look at me being very non-threatening.

This is some `code span` text

This is some *emphasis* text, I think.

You can add **more stars** to get more effects.

- [ ] Now, here are some checkboxes
  - [ ] This one's nested, crazy
  - [ ] And it has a sub-box
  - [ ] This is a really long line that just goes on and on and on and on and on and doesn't stop and you think it's going to stop but it never does.
  + [ ] This is a plus marker
- [ ] And then we're back to non-indented

```lua
  -- This is a code block
  local foo = { "bar", "bazz", "buzz" }
  for _, s in ipairs(foo) do
    print(s)
  end
```

  ```lua
    -- This is a code block
    local foo = { "bar", "bazz", "buzz" }
    for _, s in ipairs(foo) do
      print(s)
    end
  ```
