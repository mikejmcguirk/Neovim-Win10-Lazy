-- NOTE: This works because the load option in pack.add() is set to false, meaning the plugin
-- is not sourced immediately
vim.g.qs_highlight_on_keys = { "f", "F", "t", "T" }
vim.g.qs_max_chars = 9999
